name: const_date_time
description: A drop-in replacement for Dart's DateTime class with a const constructor.
version: 0.0.1
repository: https://github.com/westy92/const-date-time
funding: https://github.com/sponsors/westy92

environment:
  sdk: '>=2.18.0 <3.0.0'

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
